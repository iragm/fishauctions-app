gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/
# Kotlin Gradle Plugin build-session scratch. Shows up as untracked only while it
# holds files, so it slips past a clean `git status` between builds.
.kotlin/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
